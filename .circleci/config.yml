version: 2.1
jobs:
  build:
    docker:
      - image: cimg/node:14.10.1
    steps:
      - checkout
      - restore_cache:
          key: v1-dependencies-{{ checksum "package.jason" }}

      - run: 
          command: npm install

      - save_cache:
          key: v1-dependencies-{{ checksum "package.jason" }}
          paths:
            - node_modules
      - run: 
          command: npm test